{
  "name": "server",
  "version": "1.0.0",
  "description": "server for document summarizer",
  "main": "app.js",
  "author": "Ritesh",
  "license": "MIT",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "format:check": "pnpm format --check",
    "format:write": "pnpm format --write",
    "tsc": "pnpm --parallel -r run tsc",
    "lint": "pnpm --parallel -r run lint",
    "build": "pnpm --parallel -r run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "typescript": "^4.4.4"
  }
}